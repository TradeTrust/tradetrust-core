import { utils } from '@tradetrust-tt/tradetrust'

const {
    isTransferableAsset,
    getAssetId,
    isWrappedV2Document,
    isWrappedV3Document,
    isRawV3Document,
    isObfuscated,
    getDocumentData,
    getIssuerAddress,
} = utils

export {
    isTransferableAsset,
    getAssetId,
    isWrappedV2Document,
    isWrappedV3Document,
    isRawV3Document,
    isObfuscated,
    getDocumentData,
    getIssuerAddress,
}
